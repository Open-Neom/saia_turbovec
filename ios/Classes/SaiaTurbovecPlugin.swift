import Flutter
import UIKit

public class SaiaTurbovecPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // FFI-only plugins do not need channel method handlers.
  }
}
